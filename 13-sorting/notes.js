const letters = ['a', 'b', 'c', 'z', 'i', 'o', 'u'];
letters.sort() // teehee answer, but may not work well on large data sets

const basket = [2, 65, 244, 32, 1, 4, 7, 8];
console.log(basket.sort()) // sorts by unicode representations, not by numerical value
